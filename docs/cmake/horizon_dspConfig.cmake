# Config file for horizon_dsp CMake package. This is tiny on purpose: we lean
# on the exported targets file to surface include paths and link interfaces.
include(${CMAKE_CURRENT_LIST_DIR}/horizon_dspTargets.cmake)
