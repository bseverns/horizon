// Studio note: mid/side math is simple sum/difference, but it’s the door to all the
// spatial tricks. This block keeps the algebra naked so you remember: mono is mid,
// excitement is side, and balancing them is songwriting more than science.
#include "MSMatrix.h"
// All methods are inline in the header.
